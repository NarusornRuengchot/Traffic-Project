# MySQL / phpMyAdmin Configuration
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=ku_src_traffic

# AI Inference Device: "cuda" = NVIDIA GPU, "cpu" = CPU only, "auto" = auto-detect
DEVICE=cuda
